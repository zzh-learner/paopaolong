# 可复制给 AI 的阶段开发提示词

本文档用于把每个阶段单独交给 AI 开发。建议每次只复制一个阶段的提示词，并要求 AI 完成后运行或说明验证结果。

## 使用方式

- 先把 `GAME_PLAN.md` 和 `AI_DEVELOPMENT_ROADMAP.md` 给 AI 作为上下文。
- 每次只选择一个阶段提示词。
- 要求 AI 不要提前开发后续阶段功能。
- 阶段完成后，让 AI 总结改动文件、验证方式和遗留风险。
- 如果阶段失败，不要进入下一阶段，先让 AI 修复当前阶段。

## 通用约束提示词

```text
请基于当前仓库继续开发泡泡龙 H5 游戏。

必须遵守：
1. 使用原生 HTML/CSS/JavaScript 和 Canvas 2D。
2. 手机竖屏优先，同时兼容桌面浏览器。
3. 不引入后端、登录、排行榜或复杂框架。
4. 每次只完成我指定的阶段，不要提前实现后续阶段。
5. 保持代码模块清晰，优先使用 src/ 下的模块文件。
6. 完成后请说明修改了哪些文件、如何本地运行、如何验收。
7. 如果发现现有代码有问题，先修复当前阶段相关问题，不要做无关重构。
```

## 阶段 0 提示词：项目初始化

```text
请完成“阶段 0：项目初始化与基础约束”。

目标：
- 建立一个最小可运行的泡泡龙 H5 项目骨架。
- 页面打开后显示全屏 Canvas。
- 支持窗口缩放和 devicePixelRatio。
- 移动端页面不滚动。
- 加载 images/游戏背景.png，加载失败时使用纯色背景兜底。

请创建或完善：
- index.html
- styles.css
- src/main.js
- src/game.js
- src/render.js
- src/assets.js

不要实现发射、碰撞、消除、得分或特殊泡泡。

验收：
- 浏览器打开页面能看到稳定画布。
- 控制台没有明显报错。
- 调整窗口大小后画布跟随更新。
- 手机竖屏页面不滚动。
```

## 阶段 1 提示词：六边形网格和静态泡泡

```text
请在现有项目基础上完成“阶段 1：六边形网格和静态泡泡”。

目标：
- 顶部生成错位行六边形泡泡矩阵。
- 普通泡泡先用 Canvas 圆形绘制，不要接入泡泡贴图。
- 支持不同屏幕宽度下的合理布局。

请实现：
- src/grid.js
- gridToWorld(row, col)
- getNeighbors(row, col)
- 初始 6 行泡泡
- 4 种普通颜色随机生成
- 危险线
- 底部发射区域

不要实现发射、碰撞、消除、挂钩掉落或完整游戏循环。

验收：
- 顶部泡泡排列整齐。
- 奇偶行错位正确。
- 泡泡不明显重叠。
- 改变浏览器宽度后布局仍合理。
```

## 阶段 2 提示词：瞄准、发射和反弹

```text
请在现有项目基础上完成“阶段 2：瞄准、发射和反弹”。

目标：
- 玩家可以用鼠标或触摸瞄准。
- 松手后当前泡泡飞出。
- 泡泡碰到左右墙会反弹。
- 显示与实际弹道一致的辅助瞄准线。

请实现：
- 当前泡泡和下一个泡泡
- 发射器位置
- pointerdown / pointermove / pointerup 输入
- 发射角度限制
- 飞行泡泡状态
- deltaTime 位移更新
- 左右墙反弹
- 1 到 2 次反弹预览线
- 发射过程中输入锁定

可以新增：
- src/physics.js
- src/input.js

不要实现吸附、消除、得分、失败胜利或特殊泡泡。

验收：
- 拖动可以改变瞄准方向。
- 松手后泡泡按方向飞出。
- 泡泡碰墙后正确反弹。
- 发射中不会重复生成泡泡。
- 瞄准线和实际飞行方向基本一致。
```

## 阶段 3 提示词：碰撞和网格吸附

```text
请在现有项目基础上完成“阶段 3：碰撞和网格吸附”。

目标：
- 飞行泡泡命中顶部或已有泡泡后，吸附到最近合法网格。
- 吸附后切换下一个泡泡，允许继续发射。

请实现：
- 顶部边界碰撞
- 与已有泡泡的圆形碰撞
- worldToNearestCell(x, y)
- 候选空格搜索
- 已占用格过滤
- 吸附失败时使用邻居空格兜底
- 吸附后状态回到可瞄准

不要实现同色消除、挂钩掉落、得分、失败胜利或特殊泡泡。

验收：
- 命中已有泡泡后进入网格。
- 命中顶部后进入顶部附近网格。
- 不吸附到已占用格。
- 不出现明显重叠或穿透。
- 连续发射多次仍能正常吸附。
```

## 阶段 4 提示词：同色消除和基础得分

```text
请在现有项目基础上完成“阶段 4：同色消除和基础得分”。

目标：
- 新泡泡吸附后检查同色连通块。
- 3 个及以上同色连通时消除。
- 显示总分和本次得分提示。

请实现：
- findSameColorCluster(row, col)
- 3 个及以上同色泡泡消除
- 少于 3 个不消除
- 每消除 1 个泡泡得 10 分
- 总分 HUD
- 本次得分提示
- 简单破裂、淡出或粒子效果

可以新增：
- src/scoring.js
- src/effects.js

不要实现挂钩掉落、完整失败胜利流程或特殊泡泡。

验收：
- 3 个同色连通正确消除。
- 4 个及以上同色连通全部消除。
- 不足 3 个不会消除。
- 总分和提示分数正确。
- 消除动画后网格状态正确。
```

## 阶段 5 提示词：挂钩掉落

```text
请在现有项目基础上完成“阶段 5：挂钩掉落”。

目标：
- 同色消除后，失去顶部连接的泡泡会掉落。
- 掉落泡泡提供额外得分。

请实现：
- findFloatingBubbles()
- 从第一行泡泡 BFS 或 DFS 标记连顶泡泡
- 未连顶泡泡从网格移除
- 掉落动画
- 每个掉落泡泡得 20 分
- 本次得分提示包含同色消除分和掉落分

不要实现特殊泡泡、排行榜或复杂关卡。

验收：
- 消除连接点后，下方悬空泡泡会整体掉落。
- 仍连接顶部的泡泡不会误删。
- 掉落得分正确。
- 掉落动画期间不会允许异常重复发射。
```

## 阶段 6 提示词：完整游戏循环

```text
请在现有项目基础上完成“阶段 6：完整游戏循环”。

目标：
- 游戏可以从开始玩到失败或胜利。
- 支持暂停、继续、重新开始和结算。
- 实现无尽模式的基础难度推进。

请实现：
- loading / ready / aiming / shooting / resolving / paused / gameOver / win / settlement 状态
- 暂停和继续按钮
- 重新开始
- 失败条件：泡泡触达危险线或行数超过上限
- 胜利条件：清空普通泡泡
- 连续若干次未消除后新增一行或顶部下压
- 随进度增加颜色数量
- 游戏结束结算弹窗

不要实现特殊泡泡或排行榜。

验收：
- 可以完整玩一局。
- 失败后能重开。
- 胜利后能重开。
- 暂停后输入和动画都暂停。
- 难度会逐步增加。
```

## 阶段 7 提示词：视觉、音效和移动端手感

```text
请在现有项目基础上完成“阶段 7：视觉、音效和移动端手感”。

目标：
- 提升第一版游戏完成度。
- 优先保证移动端手感和清晰反馈。

请实现：
- 使用 images/游戏背景.png 作为背景
- 优化普通泡泡的 Canvas 质感
- 优化发射器表现
- 优化瞄准线和反弹预览
- 使用 Web Audio 生成发射、碰撞、消除、掉落音效
- 音效开关
- navigator.vibrate 震动反馈，不支持时自动忽略
- 小屏幕和横屏的基本适配

不要实现特殊泡泡、排行榜或复杂关卡。

验收：
- 移动端瞄准和松手发射响应直接。
- 音效开关可用。
- 不支持震动的浏览器不会报错。
- UI 不明显重叠。
- 视觉上接近完整小游戏。
```

## 阶段 8 提示词：测试、修复和发布准备

```text
请在现有项目基础上完成“阶段 8：测试、修复和发布准备”。

目标：
- 修复第一版明显问题。
- 准备静态发布。

请实现：
- 本地静态服务运行说明
- 调试开关：显示网格、碰撞圈或 FPS
- 检查桌面 Chrome 体验
- 检查手机竖屏体验
- 连续发射 100 次的稳定性检查
- 修复发现的布局、输入、吸附、消除、掉落、结算问题
- 优化图片加载和资源路径

不要新增大功能。

验收：
- 本地服务打开即可游玩。
- 移动端页面不滚动、不输入失灵。
- 连续游玩没有明显逻辑错误。
- 控制台没有持续报错。
- 可以交给别人试玩。
```

## 第二阶段扩展提示词：特殊泡泡

```text
请在核心玩法稳定的基础上开发第二阶段特殊泡泡。

本次只实现我指定的一种特殊泡泡，不要一次性实现全部。

统一要求：
- 特殊泡泡触发后必须执行挂钩掉落检测。
- 特殊泡泡不能破坏普通泡泡的发射、反弹、吸附、消除流程。
- 特殊泡泡需要明显视觉区别。
- 先保证规则正确，再优化素材。

可选功能：
1. 炸弹泡泡：命中吸附后清除周围一圈泡泡。
2. 彩虹泡泡：作为最优颜色参与同色消除。
3. 激光泡泡：沿飞行路径清除或穿透一条线上的泡泡。

请先说明你准备实现哪一种，然后只开发这一种。
```

