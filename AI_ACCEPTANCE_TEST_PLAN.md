# 泡泡龙 H5 阶段验收与测试计划

本文档用于每个开发阶段结束后验收。建议让 AI 完成阶段开发后，按对应章节逐项自测并报告结果。

## 通用验收规则

每个阶段都必须检查：

- 页面能打开。
- 控制台没有持续报错。
- Canvas 没有空白。
- 窗口尺寸变化后画面仍可用。
- 新增功能不破坏上一阶段功能。
- AI 必须说明本阶段修改文件和未完成事项。

## 阶段 0 验收：项目初始化

检查项：

- `index.html` 存在并能打开。
- `styles.css` 生效。
- `src/main.js` 正常加载。
- Canvas 填满可视区域。
- `devicePixelRatio` 适配后画面不模糊。
- 加载 `images/游戏背景.png`。
- 背景加载失败时有兜底绘制。
- 页面不可滚动。

通过标准：

- 桌面浏览器和手机竖屏都能看到稳定画布。
- 控制台无持续错误。

## 阶段 1 验收：网格和静态泡泡

检查项：

- 初始泡泡行数为 6 行左右。
- 泡泡颜色来自 4 种普通颜色。
- 奇偶行水平错位。
- 同一行泡泡间距一致。
- 泡泡不会明显越出左右边界。
- 危险线位于发射器上方合理位置。
- 底部留有足够发射空间。

重点边界：

- 窗口宽度约 360px。
- 窗口宽度约 430px。
- 桌面宽窗口。

通过标准：

- 网格排列稳定，视觉上符合泡泡龙顶部矩阵。

## 阶段 2 验收：瞄准、发射和反弹

检查项：

- 鼠标拖动或触摸拖动能改变瞄准方向。
- 向下拖动不会产生向下发射。
- 近水平角度会被限制。
- 松手后只发射一颗泡泡。
- 飞行速度不随帧率明显变化。
- 左墙反弹后继续向右飞。
- 右墙反弹后继续向左飞。
- 发射过程中无法再次发射。
- 瞄准线最多显示 1 到 2 次反弹。

通过标准：

- 实际飞行路线与瞄准预览基本一致。
- 连续多次发射没有输入状态错乱。

## 阶段 3 验收：碰撞和吸附

检查项：

- 直接命中已有泡泡后吸附。
- 反弹后命中已有泡泡后吸附。
- 命中顶部后吸附。
- 吸附位置为空格。
- 吸附后 `row`、`col` 合法。
- 吸附后切换下一个泡泡。
- 连续发射 20 次不出现明显重叠。

重点边界：

- 命中最左侧泡泡。
- 命中最右侧泡泡。
- 命中顶部边缘。
- 命中两个泡泡之间的缝隙。

通过标准：

- 不出现穿透、重叠、吸附到已占用格。

## 阶段 4 验收：同色消除和得分

检查项：

- 3 个同色连通会消除。
- 4 个同色连通会全部消除。
- 5 个或更多同色连通会全部消除。
- 2 个同色不会消除。
- 不同色相邻不会误消除。
- 每个消除泡泡增加 10 分。
- 本次得分提示与实际得分一致。
- 总分累计正确。
- 消除动画结束后网格状态正确。

通过标准：

- 同色消除只由新吸附泡泡触发。
- 分数和网格移除结果一致。

## 阶段 5 验收：挂钩掉落

检查项：

- 消除连接点后，下面悬空泡泡掉落。
- 悬空泡泡不再留在网格中。
- 仍连接顶部的泡泡不会掉落。
- 掉落泡泡每个增加 20 分。
- 同色消除分和掉落分同时计入本次得分。
- 掉落动画结束后才能进入下一次瞄准。

重点场景：

- 单个悬空泡泡。
- 一整块悬空泡泡。
- 左右两块中只有一块悬空。
- 消除后没有悬空泡泡。

通过标准：

- 从顶部 BFS 或 DFS 的结果与掉落结果一致。

## 阶段 6 验收：完整游戏循环

检查项：

- 开始游戏。
- 暂停游戏。
- 继续游戏。
- 重新开始。
- 失败后显示结算。
- 胜利后显示结算。
- 结算后可以重开。
- 连续未消除后会新增一行或顶部下压。
- 难度会逐步提升。
- 游戏状态不会卡在 `shooting` 或 `resolving`。

通过标准：

- 可以从开始到结束完整玩一局。
- 所有主要状态都能正常进入和退出。

## 阶段 7 验收：视觉和手感

检查项：

- 背景图正确显示。
- 普通泡泡颜色区分清楚。
- 发射器位置稳定。
- 当前泡泡和下一个泡泡易于识别。
- 瞄准线清楚但不遮挡主要画面。
- 发射音效可听见。
- 碰撞音效可听见。
- 消除音效可听见。
- 掉落音效可听见。
- 音效开关能关闭所有音效。
- 震动反馈不支持时不会报错。

移动端重点：

- 手指按住和拖动不触发页面滚动。
- 松手发射响应直接。
- 底部安全区不遮挡发射器。
- 小屏幕文字和按钮不重叠。

通过标准：

- 移动端可舒服试玩，反馈清晰。

## 阶段 8 验收：测试和发布准备

检查项：

- 本地静态服务可运行。
- 所有资源路径正确。
- 刷新页面后仍能正常进入游戏。
- 连续发射 100 次无明显卡顿。
- 大量泡泡同时消除不卡死。
- 控制台没有持续错误。
- 调试开关不会影响正常游玩。
- 可部署到静态托管。

建议最终测试矩阵：

| 环境 | 必测内容 |
| --- | --- |
| 桌面 Chrome | 基础游玩、窗口缩放、控制台错误 |
| Android Chrome | 触摸瞄准、页面不滚动、音效 |
| iPhone Safari | 竖屏布局、安全区、地址栏变化 |
| 窄屏 360px | UI 不重叠、泡泡尺寸合理 |
| 高 DPR 屏幕 | Canvas 清晰、性能可接受 |

通过标准：

- 可以把本地链接或静态部署链接发给别人试玩。

## 回归测试清单

每完成一个新阶段，都至少回归这些功能：

- 页面打开。
- Canvas 显示。
- 泡泡矩阵显示。
- 瞄准方向正确。
- 发射不重复。
- 左右反弹正确。
- 吸附不重叠。
- 3 连消除正确。
- 不足 3 个不消除。
- 悬空泡泡掉落正确。
- 总分正确。
- 暂停和重开可用。

## AI 阶段报告模板

每次让 AI 开发完一个阶段后，要求它按这个格式报告：

```text
本阶段完成：
- ...

修改文件：
- ...

本地运行：
- ...

验收结果：
- 通过：...
- 未验证：...
- 发现问题：...

下一阶段建议：
- ...
```

