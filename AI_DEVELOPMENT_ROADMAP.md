# 泡泡龙 H5 分阶段开发路线图

本文档基于 `GAME_PLAN.md`，用于把游戏拆成可交给 AI 逐步实现的开发阶段。每个阶段都应该做到“可运行、可验收、可继续开发”，不要一次性要求 AI 做完整游戏。

## 总体原则

- 第一版目标是核心玩法稳定，不追求复杂关卡和特殊泡泡。
- 每个阶段完成后都要能在浏览器中打开验证。
- 新阶段只在上一阶段验收通过后开始。
- 任何阶段都不要引入后端、登录、排行榜。
- 移动端竖屏体验优先，桌面浏览器作为兼容目标。
- 普通泡泡第一版优先用 Canvas 绘制，背景可以先接入 `images/游戏背景.png`。
- 特殊泡泡、复杂素材抠图、排行榜、关卡编辑器都放到核心玩法之后。

## 阶段 0：项目初始化与基础约束

目标：建立最小可运行的 H5 项目骨架。

开发内容：

- 创建 `index.html`、`styles.css`、`src/main.js`。
- 使用原生 JavaScript 和 Canvas 2D。
- 页面全屏显示 Canvas。
- 禁止页面滚动、双击缩放影响游戏。
- 支持窗口尺寸变化和 `devicePixelRatio`。
- 加载 `images/游戏背景.png`，加载失败时使用纯色背景兜底。

建议文件结构：

```text
index.html
styles.css
src/
  main.js
  game.js
  render.js
  assets.js
```

验收标准：

- 双击 `index.html` 或通过本地静态服务打开后能看到游戏画布。
- 浏览器窗口缩放时 Canvas 尺寸正确更新。
- 手机竖屏打开时页面不滚动。
- 控制台没有明显报错。

## 阶段 1：六边形网格和静态泡泡

目标：完成顶部泡泡矩阵的基础显示。

开发内容：

- 实现错位行六边形网格。
- 实现 `gridToWorld(row, col)`。
- 实现 `getNeighbors(row, col)`。
- 生成顶部 6 行初始泡泡。
- 普通泡泡使用 4 种颜色起步。
- 绘制危险线和底部发射区域。
- 根据屏幕宽度计算泡泡半径、列数和左右边界。

建议新增或完善文件：

```text
src/grid.js
src/game.js
src/render.js
```

验收标准：

- 顶部泡泡排列整齐。
- 奇偶行错位正确。
- 泡泡之间不会明显重叠。
- 不同屏幕宽度下布局仍合理。
- 控制台可以输出或调试每个泡泡的 `row`、`col`、`color`。

## 阶段 2：瞄准、发射和反弹

目标：玩家可以操控一个泡泡发射，并看到正确反弹。

开发内容：

- 实现发射器位置。
- 显示当前泡泡和下一个泡泡。
- 使用 `pointerdown`、`pointermove`、`pointerup` 统一触摸和鼠标。
- 实现瞄准角度限制，禁止向下或近水平无效发射。
- 实现飞行泡泡。
- 使用 `deltaTime` 更新飞行位置。
- 实现左右墙反弹。
- 实现 1 到 2 次反弹预览瞄准线。
- 发射过程中锁定输入。

建议新增或完善文件：

```text
src/physics.js
src/input.js
src/game.js
src/render.js
```

验收标准：

- 鼠标或触摸拖动可以改变瞄准方向。
- 松手后泡泡按瞄准方向飞出。
- 泡泡碰到左右墙会反弹。
- 发射中再次操作不会生成第二颗飞行泡泡。
- 瞄准线和实际弹道基本一致。

## 阶段 3：碰撞和网格吸附

目标：飞行泡泡命中顶部或已有泡泡后，稳定吸附到合法网格。

开发内容：

- 检测飞行泡泡和顶部边界碰撞。
- 检测飞行泡泡和已有泡泡圆形碰撞。
- 实现 `worldToNearestCell(x, y)`。
- 实现候选空格查找。
- 命中后吸附到最近合法空格。
- 处理边界、顶部、满格、候选失败的兜底逻辑。
- 吸附后切换下一个泡泡。

验收标准：

- 泡泡命中已有泡泡后会进入网格。
- 泡泡命中顶部后会进入顶部附近网格。
- 不会吸附到已占用格。
- 不会出现两个泡泡重叠在同一格。
- 不会出现泡泡穿过泡泡仍继续飞行。

## 阶段 4：同色消除和基础得分

目标：实现 3 个及以上同色连通消除。

开发内容：

- 实现 `findSameColorCluster(row, col)`。
- 新泡泡吸附后检查同色连通块。
- 连通数量大于等于 3 时移除。
- 连通数量小于 3 时保留。
- 实现基础得分：同色消除每个泡泡 10 分。
- 显示总分。
- 显示本次得分提示，例如 `+30`。
- 增加简单破裂动画或淡出动画。

建议新增或完善文件：

```text
src/scoring.js
src/effects.js
src/grid.js
src/game.js
```

验收标准：

- 3 个同色连通时正确消除。
- 4 个或更多同色连通时全部消除。
- 不足 3 个时不会消除。
- 消除后总分正确增加。
- 消除动画结束后网格状态正确。

## 阶段 5：挂钩掉落

目标：消除连接点后，失去顶部连接的泡泡会掉落并加分。

开发内容：

- 实现 `findFloatingBubbles()`。
- 从第一行所有泡泡开始 BFS 或 DFS。
- 标记仍连接顶部的泡泡。
- 未被标记的泡泡视为悬空泡泡。
- 悬空泡泡从网格移除并播放掉落动画。
- 掉落泡泡每个 20 分。
- 掉落分数加入本次得分提示和总分。

验收标准：

- 消除上方连接点后，下方悬空泡泡会整体掉落。
- 仍连接顶部的泡泡不会误删。
- 一次消除同时触发同色分和掉落分时，分数正确。
- 掉落动画期间不会影响下一次发射状态。

## 阶段 6：完整游戏循环

目标：从开始到失败或胜利都能完整游玩。

开发内容：

- 实现游戏状态：`loading`、`ready`、`aiming`、`shooting`、`resolving`、`paused`、`gameOver`、`win`、`settlement`。
- 实现暂停和继续。
- 实现重新开始。
- 实现失败条件：泡泡触达危险线或行数超过上限。
- 实现胜利条件：清空普通泡泡。
- 实现无尽模式难度推进：连续未消除若干次后新增一行或顶部下压。
- 随分数或发射次数增加颜色数量。

验收标准：

- 可以连续游玩一整局。
- 失败后显示结算弹窗。
- 胜利后显示结算弹窗。
- 结算后可以重新开始。
- 暂停后游戏循环、输入、动画都暂停。

## 阶段 7：视觉、音效和移动端手感

目标：让第一版看起来和玩起来像完整小游戏。

开发内容：

- 接入 `images/游戏背景.png` 作为全屏背景。
- 发射器第一版可使用 Canvas 绘制，或处理 `images/气泡发射器.png` 后接入。
- 优化泡泡高光、阴影和颜色区分度。
- 优化瞄准线和反弹预览。
- 使用 Web Audio 生成发射、碰撞、消除、掉落音效。
- 添加音效开关。
- 使用 `navigator.vibrate` 添加轻量震动反馈。
- 优化移动端触摸响应。

验收标准：

- 第一眼能识别为完成度较高的泡泡龙小游戏。
- 移动端瞄准、松手发射响应直接。
- 音效开关可用。
- 不支持震动的浏览器不会报错。
- 横屏或小屏幕不会出现严重 UI 重叠。

## 阶段 8：测试、修复和发布准备

目标：把第一版稳定到可以分享试玩。

开发内容：

- 增加基础调试开关，例如显示网格、碰撞圈、FPS。
- 测试桌面 Chrome。
- 测试 Android Chrome。
- 测试 iPhone Safari。
- 连续发射 100 次，观察是否卡顿或状态错乱。
- 修复吸附、消除、掉落、失败判定的边界问题。
- 压缩或优化图片加载。
- 准备静态部署方式。

验收标准：

- 本地静态服务可直接打开游玩。
- 移动端页面不滚动、不错位。
- 连续游玩没有明显逻辑错误。
- 控制台没有持续报错。
- 游戏失败、胜利、重开流程稳定。

## 第二阶段扩展：特殊泡泡

此阶段只在核心玩法稳定后开始。

功能顺序：

1. 炸弹泡泡：命中吸附后清除周围一圈泡泡。
2. 彩虹泡泡：作为最优颜色参与同色消除。
3. 激光泡泡：沿飞行路径清除或穿透一条线上的泡泡。

统一要求：

- 特殊泡泡触发后必须继续执行挂钩掉落检测。
- 特殊泡泡必须有独立图标或明显视觉区别。
- 特殊泡泡不能破坏普通泡泡的吸附和消除流程。
- 先实现规则正确，再接入素材精修。

## 推荐开发顺序

如果只想尽快得到可玩版本，按这个顺序推进：

1. 阶段 0 到阶段 3：先让泡泡能发射并吸附。
2. 阶段 4 到阶段 5：让核心策略成立。
3. 阶段 6：补完整局流程。
4. 阶段 7 到阶段 8：打磨和发布。
5. 第二阶段扩展：再做特殊泡泡。

