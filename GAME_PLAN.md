# 泡泡龙 H5 游戏开发计划

## 1. 项目目标

做一个可以直接在手机浏览器中打开游玩的泡泡龙 H5 小游戏。核心体验是：底部发射泡泡，顶部泡泡采用六边形网格排列，命中后吸附到网格；同色 3 个及以上连通时消除；如果消除后有泡泡不再连接顶部，则触发挂钩掉落并给予额外得分。

优先目标：

- 无登录、无后端、点击页面即可玩。
- 手机浏览器优先，同时兼容桌面浏览器。
- 操作简单：触摸/鼠标拖动瞄准，松开发射。
- 画面干净，使用现有 `images` 素材提升视觉完成度。
- 游戏逻辑稳定，尤其是反弹、吸附、连通消除和挂钩掉落。

## 2. 推荐技术方案

建议使用原生 Web 技术实现：

- `HTML`：页面入口和基础结构。
- `CSS`：全屏布局、安全区适配、移动端按钮和 HUD。
- `JavaScript`：游戏循环、物理、网格、碰撞、得分、状态管理。
- `Canvas 2D`：绘制泡泡、轨迹、发射器、分数提示和动画。

理由：

- 泡泡龙属于 2D 网格+碰撞游戏，Canvas 2D 足够。
- 不需要复杂框架即可实现，加载快，适合 H5 小游戏。
- 后续如果需要接入微信/抖音小游戏，也可以把核心逻辑迁移出去。

初始文件建议：

- `index.html`：游戏页面入口。
- `styles.css`：页面和移动端布局样式。
- `src/main.js`：初始化、资源加载、游戏循环。
- `src/game.js`：游戏主状态和流程控制。
- `src/grid.js`：六边形网格、坐标换算、吸附逻辑。
- `src/physics.js`：发射、反弹、碰撞检测。
- `src/scoring.js`：得分和连击规则。
- `src/render.js`：Canvas 绘制。
- `src/assets.js`：图片素材加载。

如果希望更快启动，也可以先做成单文件 `index.html` + `src/main.js`，等功能稳定后再拆分模块。

## 3. 核心玩法设计

### 3.1 发射流程

1. 游戏开始时生成顶部泡泡矩阵。
2. 底部中央显示发射器、当前主球和下一个次球。
3. 玩家按住屏幕或鼠标移动瞄准。
4. 显示一条可反弹的辅助瞄准线。
5. 玩家松手后，当前泡泡沿瞄准方向飞出。
6. 泡泡碰到左右边界时按入射角反弹。
7. 泡泡碰到顶部或已有泡泡时，吸附到最近的合法网格位置。
8. 检查同色连通消除。
9. 检查失去顶部连接的挂钩泡泡并掉落。
10. 更新得分，切换下一个泡泡。

### 3.2 六边形网格

泡泡采用错位行六边形网格：

- 每一行横向排列泡泡。
- 奇偶行水平错开半个泡泡半径。
- 泡泡邻居最多 6 个。
- 顶部初始生成 6 到 8 行，底部留出发射空间。

关键函数：

- `gridToWorld(row, col)`：网格坐标转 Canvas 坐标。
- `worldToNearestCell(x, y)`：命中点转最近合法网格位置。
- `getNeighbors(row, col)`：获取六方向邻居。
- `findSameColorCluster(row, col)`：从新泡泡开始查找同色连通块。
- `findFloatingBubbles()`：从顶部所有泡泡 BFS，找出未连接顶部的泡泡。

## 4. 物理与碰撞规则

### 4.1 发射速度

- 泡泡使用固定速度，避免不同设备帧率导致手感变化。
- 使用 `deltaTime` 更新位置：`position += velocity * deltaTime`。
- 最低角度限制：不能向下发射，也不能太接近水平线，防止无效弹射。

### 4.2 边界反弹

左右边界反弹规则：

- 如果泡泡中心 `x - radius <= leftWall`，则修正位置到边界内，并让 `velocity.x = abs(velocity.x)`。
- 如果泡泡中心 `x + radius >= rightWall`，则修正位置到边界内，并让 `velocity.x = -abs(velocity.x)`。

瞄准线预览需要模拟相同反弹逻辑：

- 采样射线路径。
- 最多显示 1 到 2 次反弹。
- 轨迹点遇到已有泡泡或顶部后停止。

### 4.3 泡泡碰撞

飞行泡泡与已有泡泡碰撞：

- 遍历当前网格中的泡泡。
- 如果两圆心距离 `<= bubbleRadius * 2 - tolerance`，判定碰撞。
- 碰撞后不直接放在碰撞点，而是吸附到最近的空网格。

吸附策略：

- 根据碰撞点或飞行泡泡中心寻找附近候选格。
- 过滤已经占用的格子。
- 选择距离当前泡泡中心最近的合法格。
- 如果候选失败，使用碰撞泡泡周围的空邻居兜底。

## 5. 消除与挂钩掉落

### 5.1 同色消除

新泡泡吸附后，从该格开始查找同色连通块：

- 连通数量 `>= 3`：全部破裂消除。
- 连通数量 `< 3`：保留泡泡，不触发同色消除。

破裂效果：

- 短暂放大。
- 透明度降低。
- 弹出 `+N` 分数提示。
- 播放轻量粒子或圆形扩散效果。

### 5.2 挂钩掉落

同色消除后，重新计算哪些泡泡仍然与顶部相连：

1. 从第一行所有存在泡泡的位置开始 BFS/DFS。
2. 标记所有可达泡泡为“仍挂住”。
3. 未被标记的泡泡即为失去支撑的泡泡。
4. 这些泡泡进入掉落动画，并从网格中移除。

这个机制是高分策略核心，需要优先保证准确。

## 6. 得分系统

基础规则建议：

- 同色消除：每个泡泡 `10` 分。
- 一次消除数量越多，给额外奖励：`count * count` 或 `count * 5`。
- 挂钩掉落：每个掉落泡泡 `20` 分。
- 连击奖励：连续多次发射都产生消除时，乘以 `combo`。

屏幕提示：

- 同色消除弹出实际得分，例如 `+30`、`+80`、`+220`。
- 总分显示在右下角或底部 HUD。
- 挂钩掉落可显示更醒目的 `DROP +N` 或直接显示 `+分数`。

推荐先实现简单可读版本：

- 消除提示显示本次操作获得的实际分数。
- 右下角显示真实总分。
- 后续再调分数公式。

## 7. UI/UX 设计

### 7.1 页面布局

手机优先布局：

- Canvas 占满屏幕。
- 顶部：游戏区域和泡泡矩阵。
- 底部：发射器、当前泡泡、下一个泡泡、分数。
- 右上角：暂停/重开按钮。

注意事项：

- 适配竖屏手机，优先 `360x640` 到 `430x932`。
- 使用 `100dvh`，处理移动浏览器地址栏变化。
- 保留底部安全区：`env(safe-area-inset-bottom)`。
- 防止页面滚动和双击缩放影响游戏。

### 7.2 操作方式

移动端：

- 按住或拖动屏幕任意位置瞄准。
- 松开发射。
- 发射过程中锁定输入，避免同时发射多个泡泡。

桌面端：

- 鼠标移动瞄准。
- 点击或松开发射。

### 7.3 视觉风格

使用已有素材：

- `images/游戏背景.png`：可作为全屏背景。
- `images/气泡发射器.png`：可用于底部发射器。
- `images/各色泡泡.png` 或 `images/彩色泡泡.png`：提取/裁切或作为泡泡贴图参考。
- `images/激光泡泡.png`、`images/炸弹泡泡.png`：可作为后续特殊泡泡。

如果素材是整图而不是独立 spritesheet，第一版可以先用 Canvas 渐变圆形绘制泡泡，后续再精细接入图片资源。

### 7.4 当前素材盘点

`images` 目录当前共有 13 个文件，按用途整理如下：

| 文件 | 尺寸 | 初步用途 | 备注 |
| --- | ---: | --- | --- |
| `游戏背景.png` | 1024x1536 | 游戏竖屏背景 | 深紫色科幻风格，可直接作为第一版全屏背景。 |
| `各色泡泡.png` | 1536x1024 | 普通泡泡参考/裁切素材 | 包含红、蓝、绿、黄、紫、青 6 个宝石质感泡泡。 |
| `气泡发射器.png` | 1402x1122 | 底部发射器 | 科幻机械风格，当前带白底，接入前需要抠图或用遮罩处理。 |
| `彩色泡泡.png` | 1408x768 | 彩虹/万能泡泡 | 适合作为第二阶段特殊泡泡。 |
| `激光泡泡.png` | 1408x768 | 激光泡泡 | 适合作为第二阶段特殊泡泡。 |
| `炸弹泡泡.png` | 1408x768 | 炸弹泡泡 | 适合作为第二阶段特殊泡泡。 |
| `微信图片_20260602110647_191_33.jpg` | 1080x1080 | 非核心游戏素材 | 玩偶照片，不建议放入第一版泡泡龙核心画面。 |
| `微信图片_20260602110648_192_33.jpg` | 1080x1080 | 非核心游戏素材 | 玩偶照片，不建议放入第一版泡泡龙核心画面。 |
| `微信图片_20260602110649_193_33.jpg` | 960x960 | 非核心游戏素材 | 玩偶照片，不建议放入第一版泡泡龙核心画面。 |
| `微信图片_20260602110650_194_33.jpg` | 1279x1279 | 非核心游戏素材 | 玩偶照片，不建议放入第一版泡泡龙核心画面。 |
| `微信图片_20260602110651_195_33.jpg` | 1080x1080 | 非核心游戏素材 | 玩偶照片，不建议放入第一版泡泡龙核心画面。 |
| `微信图片_20260602110653_196_33.jpg` | 1279x1279 | 非核心游戏素材 | 玩偶照片，不建议放入第一版泡泡龙核心画面。 |
| `微信图片_20260602110654_197_33.jpg` | 1279x1279 | 非核心游戏素材 | 玩偶照片，不建议放入第一版泡泡龙核心画面。 |

接入注意事项：

- 当前 PNG 基本不是可直接叠加的透明精灵图；普通泡泡、特殊泡泡和发射器仍带黑底或白底。
- 第一版建议继续使用 Canvas 绘制普通泡泡，先接入 `游戏背景.png` 提升整体观感。
- 若要接入泡泡图片，需要先把 `各色泡泡.png` 切成单独颜色精灵，并处理透明背景。
- `彩色泡泡.png`、`激光泡泡.png`、`炸弹泡泡.png` 可等核心玩法稳定后再抠图、压缩并接入特殊泡泡逻辑。
- 发射器素材需要优先处理白底，否则会在深色背景上显得突兀。

## 8. 游戏状态与失败/胜利条件

状态建议：

- `loading`：加载图片资源。
- `ready`：等待玩家开始。
- `aiming`：玩家瞄准。
- `shooting`：泡泡飞行中。
- `resolving`：消除、掉落、计分动画处理中。
- `paused`：暂停，可继续游戏。
- `gameOver`：泡泡压到底部或无可用空间。
- `win`：清空全部泡泡。
- `settlement`：结算弹窗，显示本局得分，并提供重新开始或退出到开始界面。

失败条件：

- 任意泡泡到达发射器上方的危险线。
- 或固定泡泡行数超过最大行。

胜利条件：

- 网格内没有任何普通泡泡。

## 9. 难度与关卡推进

第一版可做无尽模式：

- 初始 6 行泡泡。
- 每发射若干次未消除，顶部下压或新增一行。
- 随分数提高，颜色数量从 4 种增加到 5 或 6 种。

后续可扩展关卡模式：

- 每关固定布局。
- 指定目标分数或清空目标泡泡。
- 限制发射次数。

### 9.1 第二阶段特殊泡泡

特殊泡泡先不进入第一版核心验收，等发射、反弹、吸附、同色消除和挂钩掉落稳定后再开发。

计划加入：

- 炸弹泡泡：命中并吸附后，炸掉周围一圈或指定半径内的泡泡。
- 彩虹泡泡：可作为任意颜色参与同色 3 连消除，优先匹配能形成最大消除的颜色。
- 激光泡泡：支持反弹，并在飞行路径上穿透或切割一条线上的泡泡。

特殊泡泡触发后也要参与挂钩掉落检测，避免炸掉连接点后漏算高分。

## 10. 移动端兼容重点

必须检查：

- iPhone Safari 竖屏。
- Android Chrome 竖屏。
- 桌面 Chrome。
- 高 DPI 屏幕 Canvas 清晰度。
- 触摸事件不会导致页面滚动。
- 横屏时布局不会严重错位，可提示旋转回竖屏或自适应。

实现要点：

- Canvas 使用设备像素比缩放：`devicePixelRatio`。
- CSS 使用 `touch-action: none`。
- 用 `pointerdown / pointermove / pointerup` 统一鼠标和触摸。
- 页面设置 `overflow: hidden`。

## 11. 开发里程碑

### 阶段 1：项目骨架

- 创建 `index.html`、`styles.css`、`src/main.js`。
- 建立 Canvas 全屏渲染。
- 加载基础资源。
- 实现响应式尺寸和 DPR 适配。

验收标准：

- 手机和桌面浏览器打开页面后能看到稳定的游戏画布。
- 画布尺寸随窗口变化正确更新。

### 阶段 2：网格与静态泡泡

- 实现六边形网格坐标。
- 绘制初始泡泡矩阵。
- 实现随机颜色生成。
- 添加危险线和底部发射区域。

验收标准：

- 顶部泡泡排列整齐。
- 奇偶行错位正确。
- 不同屏幕宽度下泡泡数量和间距合理。

### 阶段 3：瞄准与发射

- 实现底部发射器。
- 实现当前泡泡和下一个泡泡。
- 实现触摸/鼠标瞄准。
- 实现泡泡飞行。
- 实现左右边界反弹。
- 实现瞄准辅助线。

验收标准：

- 玩家可以拖动瞄准并发射。
- 泡泡能按角度飞行并在左右墙反弹。
- 发射中不能重复发射。

### 阶段 4：碰撞与吸附

- 实现飞行泡泡与顶部/已有泡泡碰撞。
- 实现最近合法网格吸附。
- 处理边缘、顶部和满格异常。

验收标准：

- 泡泡命中后总是落入网格。
- 不出现重叠、穿透或吸附到已占用格。

### 阶段 5：同色消除

- 实现同色连通块搜索。
- 实现 3 个及以上消除。
- 增加破裂动画和分数提示。
- 更新总分。

验收标准：

- 新泡泡连接同色 3 个及以上时正确消除。
- 少于 3 个时不消除。
- 分数和提示正确显示。

### 阶段 6：挂钩掉落

- 从顶部泡泡开始查找仍连接的泡泡。
- 找出失去支撑的泡泡。
- 实现掉落/爆炸动画。
- 添加额外得分。

验收标准：

- 消除连接点后，下方不连顶的泡泡会整体掉落。
- 仍连接顶部的泡泡不会误删。

### 阶段 7：完整游戏循环

- 加入失败/胜利判断。
- 加入重新开始和暂停。
- 加入顶部下压或新增行。
- 调整难度和分数公式。

验收标准：

- 可以连续游玩一局完整游戏。
- 游戏结束后可以重开。
- 难度会逐步增加。

### 阶段 8：视觉与手感打磨

- 接入现有图片素材。
- 调整泡泡质感、背景、发射器。
- 添加轻量动画、音效开关、震动反馈。
- 当前没有音频素材时，先用 Web Audio 生成简单的发射、碰撞、消除、掉落音效。
- 震动反馈使用 `navigator.vibrate`，不支持的浏览器自动忽略。
- 优化瞄准线和反弹预览。

验收标准：

- 第一眼能看出是完成度较高的泡泡龙小游戏。
- 移动端操作响应直接，发射和消除反馈清楚。

### 阶段 9：测试与发布

- 本地启动静态服务器。
- 使用浏览器和手机测试。
- 检查不同屏幕尺寸。
- 修复明显布局、碰撞和性能问题。

验收标准：

- 本地链接可直接打开游玩。
- 移动端没有滚动、错位、输入失灵等问题。
- 连续游玩不会出现明显卡顿或逻辑错误。

## 12. 测试清单

功能测试：

- 直接命中已有泡泡。
- 反弹后命中泡泡。
- 命中顶部吸附。
- 3 个同色消除。
- 4 个及以上同色消除。
- 消除连接点触发挂钩掉落。
- 不足 3 个不消除。
- 泡泡到达危险线游戏结束。
- 清空泡泡胜利。
- 暂停、继续、重开。

移动端测试：

- 触摸拖动瞄准。
- 松手发射。
- 发射过程中再次触摸不会异常。
- 页面不滚动。
- 地址栏收起/展开时画布仍正确。
- 横屏不破坏游戏。

性能测试：

- 连续发射 100 次没有明显掉帧。
- 同时消除大量泡泡时动画不卡。
- Canvas 在高 DPR 手机上清晰且性能可接受。

## 13. 已确认决策

根据最新确认，当前版本按下面规则推进：

- 第一版先做无尽模式，不做复杂关卡。
- 游戏需要暂停和继续功能。
- 分数提示显示实际得分，不显示单纯消除数量。
- 需要音效和震动反馈；没有音频素材时，先用 Web Audio 生成基础音效。
- 特殊泡泡包括炸弹泡泡、彩虹泡泡、激光泡泡，但放在核心玩法稳定后的第二阶段。
- 暂时不做排行榜，后续再开发。
- 游戏失败后显示结算弹窗，可重新开始，也可退出到开始界面。

## 14. 我的补充建议

- 第一版先不要追求复杂关卡，优先把发射、反弹、吸附、同色消除、挂钩掉落做稳。
- 挂钩掉落是最能体现策略性的机制，应该作为核心验收点，而不是后期装饰。
- 移动端手感比视觉特效更重要，瞄准线、反弹预览和松手发射需要重点调试。
- 分数规则先简单计算，后续根据试玩反馈再增加连击和特殊奖励。
- 特殊泡泡作为第二阶段功能，避免干扰核心玩法稳定性。
